# 델타를 이용한 연산(슈도코드드)

arr[0…N-1][0…N-1] # NxN 배열
di[] ← [0, 1, 0, -1] 
dj[] ← [1, 0, -1, 0]

for i : 0 -> N-1
    for j : 0 -> N-1 
	    for d : 0 -> 3
	        ni ← i + di[d]
            nj ← j + dj[d]
	        if 0<=ni<N and 0<=nj<N   # 유효한 인덱스면
		        f(arr[ni][nj])
