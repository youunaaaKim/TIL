n - len(arr)
for i in range(n):
    for j in range(i):
        #오름차순으로 정렬하기 위하여 부등호 방향을 바꿔야 한다.
        if arr[j] > arr[j+1]:
            arr[j], arr[j+1] = arr[j+1], arr[j] # == 는 값이 같은지를 확인할 때 사용한다. 위치를 바꾸기 위해선 = 를 사용해야 한다.